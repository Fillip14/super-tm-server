export enum Table {
  AUTH = 'auth',
  USERS = 'users',
}

export enum Column {
  USER_ID = 'user_id',
  PROVIDER = 'email',
  PASSWORD_HASH = 'password_hash',
  ROLE = 'role',
  STATUS = 'status',
  UUID = 'uuid',
  PRODUCT = 'product',
  ACTIVE = 'active',
}
